export const EXTENSION_ID = "css3d";

/** @typedef {"panel" | "orbit" | "auto"} Css3dPointerPolicy */

export const DEFAULT_POINTER_POLICY = "panel";

export const DEFAULT_IFRAME_SANDBOX = "allow-scripts allow-same-origin allow-forms allow-popups";

export const DEFAULT_PANEL_WIDTH_PX = 320;
export const DEFAULT_PANEL_HEIGHT_PX = 200;
