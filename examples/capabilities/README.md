# Stable capability examples

`lod-curves.json` exercises the first-class LOD object and the shared curve descriptor used by Line, Tube, and declarative path animation. Morph targets depend on the loaded model, so the engine-level behavior is covered by `tests/m4Capabilities.test.mjs` and the `morph.list` / `morph.set` command examples in the API.
