# editor/lib — copied from tools/common/editor-single/

Frozen baseline: `tools/common/editor-single/` is not imported at runtime.

Regenerate:

```bash
node tools/scene-host/scripts/copy-scene-editor-lib.mjs
```
