/**
 * nature root domain: sky, water, etc. (capabilities in nature.sky / nature.water subdomains).
 * Do not invoke domain: "nature" directly; use full subdomain id.
 */
const natureDomain = {
  id: "nature",
  api: {}
};

export default natureDomain;
